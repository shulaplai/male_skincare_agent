# Build context for the frontend image. Keep node_modules / dist out of the
# context — npm ci + vite build regenerate them inside the image.
node_modules
dist
.git
.gitignore
Dockerfile
.dockerignore
npm-debug.log*
.DS_Store
.env
.env.*
*.md
