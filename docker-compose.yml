services:
  backend:
    build: ./backend
    ports:
      - "8001:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
